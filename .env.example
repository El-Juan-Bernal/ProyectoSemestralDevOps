# Copia este archivo como ".env" en la misma carpeta que docker-compose.yml
# y reemplaza los valores. El archivo ".env" real NO debe subirse a GitHub
# (agrégalo a tu .gitignore si no está ya).

MYSQL_ROOT_PASSWORD=CambiaEstaClaveRoot123
DB_USERNAME=app_user
DB_PASSWORD=CambiaEstaClaveApp123
